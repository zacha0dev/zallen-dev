// index.js - Functions v4 entry point. package.json "main" points here; it
// pulls in the modules that register the HTTP functions (OAuth + MCP).
require("./functions/oauth");
require("./functions/mcp");
